services:
  - type: web
    name: doneeco-mvp
    env: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: SIGNED_URL_TTL_SECONDS
        value: "900"

databases:
  - name: doneeco-db
    plan: free
